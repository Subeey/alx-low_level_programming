#include "main.h"

/**
 * print_alphabet - make  the alphabet
 *
 * Return: always 0 when success
 */

void print_alphabet(void)

{
	char f;

	for (f = 'a' ; f <= 'z' ; f++)
		_putchar(f);
	_putchar('\n');

}
